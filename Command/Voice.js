module.exports = { name: 'voice', aliases: ['tts'], execute: async(sock, msg, args) => { const text = args.join(' '); if(!text) return sock.sendMessage(msg.key.remoteJid, { text: 'Usage:.voice hello' }); const url = `https://translate.google.com/translate_tts?ie=UTF-8&q=${encodeURIComponent(text)}&tl=ur-PK&client=tw-ob`; await sock.sendMessage(msg.key.remoteJid, { audio: { url }, mimetype: 'audio/mpeg', ptt: true }); } }
