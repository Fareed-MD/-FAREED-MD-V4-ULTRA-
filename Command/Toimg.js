const { downloadMediaMessage } = require('@whiskeysockets/baileys'); module.exports = { name: 'toimg', execute: async(sock, msg, args, isOwner, PREFIX, CHANNEL, downloadMediaMessage) => { const quoted = msg.message.extendedTextMessage?.contextInfo?.quotedMessage; if(!quoted?.stickerMessage) return sock.sendMessage(msg.key.remoteJid, { text: 'Sticker ko reply karke .toimg likho' }); const buffer = await downloadMediaMessage(msg, 'buffer', {}, { reuploadRequest: sock.updateMediaMessage }); await sock.sendMessage(msg.key.remoteJid, { image: buffer }); } }
