module.exports = { name: 'imagine', aliases: ['img'], execute: async(sock, msg, args) => { const prompt = args.join(' '); if(!prompt) return sock.sendMessage(msg.key.remoteJid, { text: 'Usage:.imagine sunset' }); await sock.sendMessage(msg.key.remoteJid, { text: '🎨 Image bana raha hun...' }); const url = `https://image.pollinations.ai/prompt/${encodeURIComponent(prompt)}`; await sock.sendMessage(msg.key.remoteJid, { image: { url }, caption: `*Prompt:* ${prompt}\n*By Fareed-MD | Project by Fareed Bhatti*` }); } }
